# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Push secrets — the FCM V1 service-account key and any admin SDK JSON must never
# be committed (they authorize sending push to your devices). google-services.json
# is NOT secret and stays tracked so EAS Build can bundle it.
*-firebase-adminsdk-*.json
*service-account*.json

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# logs
*.log
